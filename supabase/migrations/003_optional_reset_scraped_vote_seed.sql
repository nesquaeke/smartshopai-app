-- Optional one-time: reset scraped seed counters so votes start organic.
-- Run in Supabase SQL Editor when you want existing rows zeroed.

-- UPDATE public.products SET upvotes = 0, downvotes = 0, engagement = 0;
